{
  "name": "focas-crm-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo workspace for Focas CRM packages.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  }
}
