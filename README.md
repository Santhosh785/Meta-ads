# Meta-ads

Monorepo for Focas CRM packages.

## Packages

- [`@santhosh785/meta-ads`](packages/meta-ads) — reusable, fetch-only connector for the Meta (Facebook) Marketing API.

## Development

```bash
npm install          # install all workspace dependencies
npm run build        # build every package
npm test --workspaces --if-present
```
